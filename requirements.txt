# -*- coding: utf-8 -*-
# SmartLabel 依赖清单
# 版本号锁定为 mmcv 环境实测可用的版本（== 精确匹配）。
# 如需放宽，可改为 >= 同主版本。

# ---- GUI ----
PyQt5==5.15.11

# ---- Web ----
fastapi==0.128.8
uvicorn==0.39.0
python-multipart==0.0.20
httpx==0.28.1                    # FastAPI TestClient 依赖
pydantic==2.13.3

# ---- CLI ----
typer==0.23.2
rich==13.4.2

# ---- VLM ----
torch==2.4.1
torchvision==0.19.1
transformers==4.57.6
accelerate==1.10.1
qwen-vl-utils==0.0.14
bitsandbytes==0.48.2             # Windows 4-bit 量化（替代 autoawq）
# autoawq>=0.2.5                 # 可选，仅 Linux + Qwen2-VL AWQ 模型用

# ---- 自有模型（可选） ----
onnxruntime-gpu==1.19.2          # 可选，使用 ONNX 自有模型时需要
# tensorrt                       # 可选，按环境单独安装

# ---- 数据处理 ----
Pillow==10.4.0
opencv-python==4.10.0.84
lxml==6.1.0
pandas==2.2.3
numpy==2.0.1

# ---- 可视化与报告 ----
matplotlib==3.9.2
jinja2==3.1.4

# ---- 评估 ----
scikit-learn==1.6.1

# ---- 工具 ----
PyYAML==6.0.1
tqdm==4.67.3
